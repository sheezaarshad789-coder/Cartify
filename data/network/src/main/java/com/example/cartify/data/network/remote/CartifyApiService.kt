package com.example.cartify.data.network.remote

/* 
 * Retrofit implementation is disabled as per user request to use only Supabase.
 * This file is kept empty to avoid build errors from existing references without deleting it.
 */

interface CartifyApiService {
    // Disabled
}

object ApiClient {
    // Disabled
}
